datasets==4.5.0
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
scipy==1.13.1
torch==2.8.0
transformers==4.57.6
sentence-transformers==5.1.2
evaluate==0.4.6
rank-bm25==0.2.2
faiss-cpu==1.13.0
tqdm==4.68.3
matplotlib==3.9.4
seaborn==0.13.2
jupyter==1.1.1
beautifulsoup4==4.15.0
deep-translator==1.11.4
requests==2.32.5

# NOTE: the following were used for 4-bit quantized LLM inference (Qwen 14B)
# on a remote GPU environment and were not captured in the local .venv snapshot.
# Pin these to the exact versions used on that environment before final submission:
# bitsandbytes==<version used>
# accelerate==<version used>
